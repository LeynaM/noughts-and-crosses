# Noughts and Crosses

A real-time, multiplayer Noughts and Crosses (Tic-Tac-Toe) game built with Python, FastAPI and WebSockets.

## Getting started

install dependencies: `uv sync`.

install commit hooks: `pre-commit install`.

run: `uv run fastapi dev`


